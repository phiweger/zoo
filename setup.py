from setuptools import setup

setup(name='zoo',
      version='0.1',
      description='A distributed microbial database',
      url='none',
      author='av',
      author_email='none',
      license='MIT',
      packages=['zoo'],
      zip_safe=False)
